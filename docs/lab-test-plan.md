# 隔离靶机测试方案

1. 使用一次性 Windows VM 和可回滚快照，关闭共享剪贴板、共享目录和凭据同步。
2. 使用非管理员账户；默认断网，必要时仅连接专用模拟网络和本地假服务。
3. 创建记录目录，核对 SHA-256，启动进程、文件/注册表和网络监控。
4. 执行最小、可回滚的测试用例，记录命令行、时间和退出码；不得使用真实账号或生产地址。
5. 保存日志和截图，停止监控，恢复快照，确认宿主机无新增文件。

不得对第三方系统扫描、投递、持久化或绕过访问控制，也不得接入生产网络。
