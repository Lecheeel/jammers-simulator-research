# Analysis artifacts

`evidence/` contains small, reviewable summaries and representative outputs from
the offline reverse-engineering pass. `embedded/` contains the Wails/WebView2
assets extracted from the sample with `extract_embedded_assets.py`.

The large raw PE dumps and transient runtime captures are intentionally not
published here. Re-run extraction against the fixed-hash sample when a fresh
copy is required.
